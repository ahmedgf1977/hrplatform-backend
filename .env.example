# Base de datos PostgreSQL (Railway te da esta URL automáticamente)
DATABASE_URL=postgresql://usuario:password@host:5432/hrplatform

# JWT Secret — cambia esto por una cadena segura
JWT_SECRET=hrplatform_super_secret_2025_zavix_adc

# Puerto (Railway lo asigna automáticamente)
PORT=3000
